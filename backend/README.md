# Installing PyKoDa

`pykoda` is not available on pypi, therefore to install it:

1. Create and activate a virtual environment
2. Clone the git repo: https://github.com/KoDa-project/pykoda.git
3. Go into the repo and run: `pip install .`
